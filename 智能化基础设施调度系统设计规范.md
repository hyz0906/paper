这是一份针对基础设施层**“任务负载监控预测与数量规划”**的深度设计文档。该文档旨在为计算平台组提供具体的实施路径，从数据特征工程到模型选择，再到执行策略的完整闭环设计。
智能化基础设施调度系统设计规范
Intelligent Infrastructure Scheduling: Load Prediction & Capacity Planning Design Specification
版本：v1.0
密级：内部公开
编制：工程效能架构组
日期：2025年12月
1. 背景与核心痛点 (Background & Problem Statement)
在Android/鸿蒙全系统构建场景下，计算资源的需求呈现极端的潮汐效应：
 * 痛点 A (资源闲置)：深夜及周末资源利用率低于 10%，大量服务器空转，电费与折旧成本高昂。
 * 痛点 B (排队拥堵)：特定时间段（如周五下午、发版日前夕、每日 14:00-16:00），Commit 提交量激增。由于资源扩容滞后，构建任务在队列中积压，严重影响研发反馈闭环。
 * 痛点 C (扩容盲目)：缺乏科学的数据支撑，服务器采购往往基于“拍脑袋”或简单的同比/环比，导致算力过剩或长期不足。
本方案旨在建立一套基于时序预测的主动式调度系统，从“被动响应负载”转向“主动预测负载”。
2. 总体设计思路 (Design Philosophy)
系统核心逻辑由 预测模型 (Predictor) 与 决策引擎 (Decision Engine) 构成：

 * 输入 (Input)：历史门禁任务数据、当前代码仓待合入队列、日历特征（工作日/节假日）、代码变更量级。
 * 预测 (Prediction)：未来 T 时间窗口内的任务到达率 (\lambda) 及所需算力 (CPU/Mem)。
 * 决策 (Decision)：计算 \Delta \text{Nodes}，触发 Cloud API 扩缩容或任务抢占。
3. 详细方案设计 (Detailed Solution Design)
3.1 数据特征工程 (Feature Engineering)
要训练高精度的预测模型，必须清洗并提取以下关键特征：
| 特征类别 | 关键指标 | 说明 |
|---|---|---|
| 时序特征 (Time Series) | day_of_week, hour_of_day, is_holiday | 捕捉周五下午高峰、午休低谷等周期性规律。 |
| 队列特征 (Queue Metrics) | current_queue_depth (当前排队数) | 实时压力的直接体现。 |
| 代码特征 (Code Context) | pending_merge_requests (待合入MR数), files_changed_count | 预示即将到来的构建风暴。例如，Framework 层的大规模重构通常意味着全量编译。 |
| 项目特征 (Project Type) | project_priority (P0/P1), build_target (全量/增量) | 不同类型的任务对资源的消耗模型不同（全量构建约需 64C/128G，增量仅需 16C）。 |
| 外部事件 (External Events) | release_countdown (发版倒计时) | 越临近发版日（Code Freeze），提交频率越呈现指数级上升。 |
3.2 预测模型架构 (Prediction Models)
我们采用组合模型策略来应对不同时间维度的需求。
A. 短期负载预测模型 (Short-term: The "Reflex")
 * 目标：预测未来 1 小时内的任务并发量。
 * 算法选择：Prophet (处理周期性) + LSTM (处理非线性序列)
   * 利用 Prophet 拟合每日/每周的基线趋势（Baseline）。
   * 利用 LSTM 结合当前 pending_merge_requests 作为滑动窗口输入，预测偏离基线的突发流量。
 * 输出：未来 60 分钟每 10 分钟粒度的任务到达数量预测值 \hat{y}_{t+1...t+6}。
B. 长期容量规划模型 (Long-term: The "Strategy")
 * 目标：预测未来 6 个月的算力缺口。
 * 算法选择：多变量线性回归 (Multivariate Linear Regression)
 * 核心公式：
   
   * 随着代码仓库体积 (\text{RepoSize}) 和开发者人数 (\text{DevCount}) 的增长，构建时长和频率呈线性或超线性增长。
 * 输出：建议采购的服务器节点数及规格（如：需新增 50 台 128核/512G 编译机）。
3.3 场景化执行策略 (Scenario Execution)
场景一：短期弹性扩容 (Short-term Elasticity)
策略名称：Look-ahead Scaling (前瞻性扩容)
 * 触发机制：
   当模型预测 \text{Load}_{t+30min} > \text{Capacity}_{\text{current}} \times 0.85 时，触发扩容信号。
   提前 30 分钟是为了覆盖云主机启动（Cold Start）及环境初始化时间。
 * 资源编排 (Spot vs. On-Demand)：
   * 编译任务 (Stateless)：申请 Spot Instances (竞价实例)。构建系统需具备断点续传能力，若 Spot 实例被回收，可重新调度不影响整体（仅单任务重试）。
   * 链接/打包任务 (Stateful)：申请 On-Demand (按需实例) 或使用本地保留集群，确保高稳定性。
 * 降级与抢占 (Preemption)：
   若公有云资源池耗尽或达到成本上限：
   * 暂停所有 P2 级任务（如：每日定时的静态检查、非阻塞性 Unit Test）。
   * 优先保障 P0 级任务（如：Release 分支构建、阻塞性门禁）的资源供给。
场景二：长期容量规划 (Long-term Planning)
策略名称：Trend-based Procurement (趋势驱动采购)
 * 数据分析：
   系统每月自动运行一次回归分析，分析过去 12 个月的代码膨胀率（Code Bloat Rate）。
   * 例如：发现 Android Framework 代码行数季度增长 15%，导致单次全量编译时间延长 8%。
 * 生成报告：
   自动生成《季度算力需求预测报告》，包含：
   * 当前水位：峰值时段 CPU 饱和度。
   * 增长预测：预计下季度峰值算力缺口（Core hours）。
   * 采购建议：推荐购买的硬件配置（侧重高频 CPU 还是大内存，取决于瓶颈分析）。
   * ROI 分析：对比“新购机器”与“租用公有云”的成本盈亏平衡点。
4. 系统架构图 (System Architecture)
graph TD
    subgraph Data_Source [数据源层]
        Git[Gitlab/Gerrit Event Stream]
        Prom[Prometheus Metrics]
        Jenkins[Jenkins/Build Logs]
        Cal[Corporate Calendar]
    end

    subgraph Data_Processing [数据处理层]
        ETL[实时特征提取 (Flink)]
        DW[特征存储 (Feature Store)]
    end

    subgraph AI_Core [AI 核心层]
        Model_S[短期预测模型 (Prophet/LSTM)]
        Model_L[长期规划模型 (Regression)]
        Train[模型训练 Pipeline (Airflow)]
    end

    subgraph Decision_Engine [决策与执行层]
        Scaler[K8s Auto-scaler (KEDA)]
        Spot_Mgr[Spot 实例管理器]
        Preempt[任务优先级调度器]
    end

    subgraph Resources [基础设施资源池]
        Local[本地私有云 (IDC)]
        Cloud[公有云弹性池 (AWS/HuaweiCloud)]
    end

    Git & Prom & Jenkins & Cal --> ETL
    ETL --> DW
    DW --> Model_S & Model_L
    DW --> Train --> Model_S
    
    Model_S -- "预测未来30min负载" --> Scaler
    Scaler -- "扩容指令" --> Spot_Mgr
    Scaler -- "资源不足" --> Preempt
    
    Spot_Mgr --> Cloud
    Preempt --> Local

5. 实施路线图 (Implementation Roadmap)
| 阶段 | 周期 | 目标 | 关键动作 |
|---|---|---|---|
| P1: 数据洞察 | M1-M2 | 建立数据基线 | 1. 部署 Prometheus Exporter 收集构建队列深度。
6. 清洗历史 1 年构建数据，分析周期性规律。
7. 实现“长期规划报告”的手动生成。 |
| P2: 影子模式 | M3-M4 | 模型验证与调优 | 1. 训练 Prophet 模型，进行影子运行（预测但不执行）。
8. 比较预测曲线与真实负载曲线，优化特征工程（如加入“代码变更量”权重）。
9. 准确率达到 85% 以上。 |
| P3: 弹性接管 | M5-M6 | 自动化落地 | 1. 对接公有云 API，开启保守策略（仅扩容不缩容，仅使用 Spot 实例）。
10. 实施 KEDA (Kubernetes Event-driven Autoscaling) 规则。
11. 验证成本节约效果。 |
12. 预期收益 (Expected Value)
 * 构建等待时长 (Queue Time)：预计降低 40%。通过周五下午的前瞻性扩容，消除“排队一小时，构建十分钟”的现象。
 * 资源成本 (Cost)：预计降低 20%-30%。
   * 夜间自动缩容至最小集群规模。
   * 利用 Spot 实例（通常主要价格的 10%-20%）承担波峰负载，而非为了波峰购买昂贵的固定资产。
 * 运维人效：消除人工监控和临时紧急扩容的操作，转为通过 Dashboard 监控 AI 决策质量。